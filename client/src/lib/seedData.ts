// FlowZone — Seed data for demo purposes
// This file is kept for compatibility but no longer populates demo data

import { getData } from './store';

export function seedDemoData(): void {
  // No-op: App starts empty for production use
  // Users can create their own data from scratch
}
